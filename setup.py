from setuptools import setup, find_packages

setup(
    name="my_boto3_extension",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="An extension of boto3 that adds extra functionality to the QBusiness client",
    url="https://github.com/yourusername/my_boto3_extension",
)
